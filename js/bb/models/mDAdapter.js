/**
 * Модель использующаяся при получении архивных данных
 * @returns {unresolved}
 */
function mDAdapter() {
    var model = Backbone.Model.extend({
        //idAttribute:"id",
        defaults:{},
        initialize: function(options){}
    });
    return model;
}


